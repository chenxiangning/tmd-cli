# 0.1.3 发布区全量代码审查(48 提交)

- 日期:2026-09-09
- 状态:报告已出;P0 口径漂移四处已随 0360da9 修正;其余整改项待拍板
- 范围:`06f5643..HEAD`(0.1.2 合入后至 093e4be,48 提交,171 文件,+7376/−995)
- 方法:三路并行专项审查(架构合规语义 / 死代码与过度设计 / 文档与实现一致性)+ 本地 CI 基线 + 主会话对 kernel/Rust 高风险面亲审;审查时点工作区干净,CI 四项绿(typecheck / arch-boundary / file-size / 1172 测试 / 153 文件)

## 结论

代码层合规,可发布。架构铁律 1-10 语义层零违规(P0 = 0),CI 查不出的深层项(注册面纪律、幕布透传、CLI 私有知识归属、通用原语边界)全部实测干净。最重问题按序:

1. 文档漂移:CHANGELOG「8 个界面图标」与实码 7 键矛盾(P0,同源四处,已修正);CHANGELOG 漏记三大特性;FEATURES.md 4 处失实行 + 缺 0.1.3 全部新特性行(P1,见第三节清单)。
2. kernel 侧两处「静态副本」型知识内联(P1,漂移温床而非现行越权,不阻塞发布,见第一节)。
3. 死代码净可删约 20 行;唯一 P1(sftpStat 三层死链)是审查区间外的既有孤儿。

## 一、架构合规(专项一)

六项语义检查:kernel 纯度 / 注册面合规 / cli-shared 准入 / R1-R3-R4 与 300 行 / PTY 幕布约束 / Rust 侧。发现 P1 x 2 + P2 x 5:

| 级别 | 位置 | 问题 | 建议处置 |
|---|---|---|---|
| P1 | src/kernel/settingsAppearance.ts:75-84 | ICON_DECOR_IDS 把面板/侧栏动作注册 id 静态投影进 kernel,同一 id 集共 3 份副本(kernel 清洗、kernel/iconDecor.ts CSS 应用、settings 插件 IconDecorCard.tsx:53-65 展示清单);插件改 id 或新图标进装饰体系须三处联动,漂移即静默失效 | 最小改法:补「白名单集合 = 注册表实际 id 投影」同步断言测试;彻底改法: id 集由注册表派生(panel- 前缀规则代码化) |
| P1 | src/kernel/settingsTypes.ts:289 + settingsSanitize.ts:69 | memory 沉淀代写引擎裸名枚举 `"omp" \| "pi" \| "opencode"` 硬编码 kernel 类型与清洗两层,属 memory-coordinator 插件私有语义内联;对照同文件 resolveCliSessionQuota 的 registeredCliIds 动态注入,静态名单使 harness 改名/新增须 kernel 联动 | kernel 留 string + 通用清洗,合法值由 memory 插件自校验;至少补同步注释与测试钉住三方一致 |
| P2 | src/kernel/ipc.ts:466 + src-tauri/src/hash.rs:2-6 | kimi 会话目录 MD5(cwd) 布局知识以注释内联 kernel 与 Rust 通用原语(命令本体通用、消费方 cli-kimi 自拼路径,行为合规,注释载体越界铁律 9) | 注释通用化,kimi 布局知识挪 cli-kimi/cli-shared 或 docs/architecture |
| P2 | src/kernel/settingsTypes.ts:169-170 | sessionTitles 注释内联 omp/pi title 定长 pad 覆写格式细节(机制合规,格式细节属适配器知识) | 注释缩为指向 docs/architecture |
| P2 | src/kernel/plugin.ts:68-69 | 「不存在旁路注册表」宣言与 kernel 中性桥的直接 import 并存(assets import registerComposerTriggerSource、workspace import registerSessionRevealHandler);桥本身合规(R4 下插件不可互 import,桥面必须落 kernel,两文件头有先例论证),宣言未区分「外壳贡献点经 ctx」与「插件间 kernel 桥可直连」两类 | 注释补一句分类说明 |
| P2 | src/app-shell/SidebarSettingsCluster.tsx:31 | DEFAULT_PINNED = ["system-proxy"] 静态引用 network-proxy 插件动作 id;插件改 id 后默认钉住静默消失(loadPinned 有降级回退) | SidebarAction 加 defaultPinned 标志由插件声明,或接受现状列检查项 |
| P2 | src/plugins/checkpoints/index.tsx:111 | activate 内全局 setInterval(4s) 无退订,插件未定义 deactivate(拔插 = 重启生效,当前无实际故障,仅禁用后轮询仍跑) | 句柄存 this.deactivate 清理 |

已查且干净(覆盖面摘要):

- R1/R3/R4 全仓 0 命中;R3 唯一例外是 app-shell/updateCheck.ts 浏览器 dev 特性探测,非 import 旁路。
- 注册面:allPlugins 23 插件全注册;插件裸调用 registerXxx 0 命中;区间新功能(assets/git/checkpoints/workspace/settings)挂载逐个核过,全经 activate(ctx)。
- cli-shared:26 文件消费矩阵逐个核对,全部满足「>=2 个 cli-* 或 1 个 cli-* + feature 联合」准入,先例声明注释齐备。
- PTY 幕布:pty_spawn 泵 = UTF-8 增量解码 + 原样 emit,零解析注入;terminalReplay 分块原样 write,进度/遮罩全在幕布外状态层;terminalHistory 整段重写;terminalReports 仅输入侧语义判定。
- Rust 侧:sqlite/installer/quota/git/hash 全通用原语,无 CLI 私有路径/表结构;checkpoints ws_dir 的 md5(cwd) 是 tmd 自身账本规则非 kimi。
- 300 行铁则全过,豁免仅 ipc.ts(头 10 行内标注,正当)。
- 主会话亲审补充:terminalReplay.ts / autoActivate.ts / iconDecor.ts / composerExt.ts 四个新 kernel 模块均为通用机制(profile 标志驱动、CSS 变量约定、纯注册表,`!!`/`##` 仅注释示例);Rust diff full 参数 / request_restart+kill_all / proc_run 宽限收窄均干净且带回归测试。

## 二、死代码与过度设计(专项二,net 约 -20 行)

| 级别 | 位置 | 内容 |
|---|---|---|
| P1 | src/kernel/ipc.ts:514 + src-tauri/src/lib.rs:220 + src-tauri/src/ssh/commands.rs:160-163 + ssh/sftp_ops.rs:86-96 | sftpStat 三层死链:前端 ipc.sftpStat 零调用者,Rust ssh_sftp_stat 命令唯一消费者是该注册行,sftp_ops::stat 唯一消费者是该命令。既有孤儿(区间外),四处一并删约 -19 行;保留 SftpEntry 类型(list/read_text 仍用) |
| P2 | src/plugins/workspace/SessionRows.tsx:33 | `export type { SessionStatus }` 再导出零消费者,整行删(类型本体在 utils.ts 保留) |
| P2 | autoActivate.ts:25 / composer/promptGate.ts:24 / checkpoints/timelineText.ts:18 / git/views/patchModel.ts:11 / cli-shared/cliQuery.ts:153 | 5 个导出类型零外部消费者(AutoActivateCandidate / PromptGateState / TimelineParts / PatchRowKind / RpcSuggestionSource),摘 export 关键字即可,行数不变 |

已查且干净:扁平化遗留(GroupHeader / useGroupCollapsed / workspace-sessions-groups.css / workspaceGroupCollapsedMap)删净;Eye 撤销与图标装饰收键零残留(键表/闪烁表/颜色变量/UI 清单四方一致);副车诊断管线(7e4472a)无残留;promptStore 拆分无双份字段;IPC 三方对账前端 96 命令 = Rust 注册集(sftpStat 孤儿除外);区间新增 CSS 类全消费;package.json 依赖全有 import;locales 新增键抽查 17 个全有消费点;无同功能双实现、无无人设置的可配置项。

## 三、文档与实现不统一(专项三,整改清单)

P0(已修正,0360da9):「8 个界面图标」实为 7 键(f2cbf1a 收键后发布面未回改),四处同源:CHANGELOG 新增节、docs/README.md 索引行、settingsTypes.ts:140 注释、icon-decor spec 应用层节(含 `eye/` 前缀残留与「8 条」计数)。

P1(待排版):

1. CHANGELOG 0.1.3 漏记三大特性:智能体/提示词资产库(fa24fa3 系,约 50 文件新插件,FEATURES 反而收录)、会话列表扁平化(70926bb,分组段头与折叠退役)、PTY 幕布加载进度条 + 大缓冲分块回放(847b4e3 / 96de3cc / 7389929)。
2. docs/FEATURES.md:38「正在查看的行左侧加眼睛标识」已失实(f2cbf1a 撤销 Eye,全仓 0 命中)。
3. docs/FEATURES.md:64「最多同时展示 4 个」已失实(sessionTabsMax 可配 1-10)。
4. docs/FEATURES.md 工作区会话域三行陈旧:「会话管理模式段头开关进入」(段头已退役,入口在工作区行动作组)、「贯穿竖线三态灰=静止」(竖线回归为树线,灰点 display:none)、「活会话固定在所属 CLI 分组顶部」(扁平化后无分组)。
5. docs/FEATURES.md 缺 0.1.3 六项新特性行(自动激活 / 时间线页签 / diffMode 双栏+全文 / 图标装饰 / 扁平化 / 幕布进度条)。

P2(择期):

- docs/architecture/02:挂点计数「10 个」实为 12(fa24fa3 新增 composer.inputRail 与 header.leftCluster 均未入图);头部版本标注仍写 v0.1.1。
- docs/architecture/05:「正在查看时引擎图标让位 Eye」句失实。
- workspace-flat spec:设计要点 4 与验证段 II 未随「竖线回归 16px」「左竖条撤销」划除,与 3c 段自相矛盾。
- git-diff spec 验证段仍指已删的 patchLines.test.ts(现况:patchModel.test.ts +139 与 PatchLines.test.tsx +39)。
- 代码注释两处灰态口径失实:workspace/index.tsx:9-10 与 SessionRows.tsx:59 写「转灰」,实为 `.tl-node.is-idle` display:none。

## 四、既往评审闭环核对

- 午后评审(15 笔,2026-09-08-postnoon):2 高 + 1 P1 全闭环(7bd17cc 遮罩就绪锁 / bc6349e request_restart / 2e0bf67 silent 链);P2 已闭 3 项,列名未闭 19 项(多数评审自标择期)。实质项择要:批量归档/删除仍逐条落盘逐行 await、全文查看无体积护栏、E_ 前缀剥离多处重复、dsh 时间线缺「引擎不支持」态、tests_commit_view.rs 的 full 分支零 Rust 测试、「更多...」分页按钮两份重复、两段式读头窗口零专测。
- 早晚场评审(19 笔,2026-09-08-evening):1 高 + 5 P1 全闭环(其中 P1-5 fa24fa3 提交信息失实归类为流程记录);P2 已闭 12 项,挂起 3 项(i18n 高频键存在性断言择期、SessionTabBar 拥挤属产品决策、流程记录项)。

## 五、建议处置顺序(供拍板)

1. 发布前(纯文档,半小时级):CHANGELOG 补三条目;FEATURES.md 改 4 处失实 + 补 6 行。
2. 随下个版本(代码小改,零风险):sftpStat 死链删除;5 个类型摘 export;checkpoints setInterval 挂 deactivate;ipc.ts/hash.rs/sessionTitles 注释通用化。
3. 需要设计决策(本报告的核心排版议题):
   - ICON_DECOR_IDS 三份副本:补同步断言测试(最小)vs 注册表派生(彻底);
   - MemoryDistillEngine 裸名枚举:kernel 通用化 + 插件自校验(彻底)vs 同步注释+测试(最小);
   - SidebarSettingsCluster DEFAULT_PINNED 静态 id:插件声明 defaultPinned vs 维持现状。
